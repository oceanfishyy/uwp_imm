<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:core="using:Microsoft.Xaml.Interactions.Core"
      x:Class="UWP_Ocean.MainPage"
      mc:Ignorable="d"
      DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!--<StackPanel Orientation="Vertical">
            <Button Command="{Binding NavigateCommand}" CommandParameter="1">1</Button>
            <Button Command="{Binding NavigateCommand}" CommandParameter="2">2</Button>
            <Button Command="{Binding NavigateCommand}" CommandParameter="3">3</Button>
            <Button Command="{Binding NavigateCommand}" CommandParameter="4">4</Button>
        </StackPanel>-->
        
        <Frame x:Name="mainFrame" Grid.Column="1"></Frame>
    </Grid>
</Page>
